'''Contains information about paths to a couple of directories.This is so code can use relative paths to inputs / outputsVariables starting with _ refer to Python pathlib objectsWithout the _, the variable should be stringThese paths can be accessed from elsewhere in the project using: (assuming you have installed it using pip install)    from src.config import env    env.DATA_DIR    env._DATA_DIR'''from os.path import basename, dirname, joinimport osfrom pathlib import Path# Determine the root directory for the package_CFG_DIR = Path(os.path.dirname(os.path.abspath(__file__)))_ROOT_DIR = _CFG_DIR.parentCFG_DIR = str(_CFG_DIR)ROOT_DIR = str(_ROOT_DIR)# Data directory_DATA_DIR = Path('/home/davidbyrne/disks/ssd500/eva/lethal_heat')DATA_DIR = str(_DATA_DIR)# Inputs directory_INPUT_DIR = _DATA_DIR / 'inputs'INPUT_DIR = str(_INPUT_DIR)# Processed data directory_PROC_DIR = _DATA_DIR / 'processed'PROC_DIR = str(_PROC_DIR)